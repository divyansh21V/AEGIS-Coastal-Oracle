# ═══════════════════════════════════════════════════════════════
# 🌐 AEGIS — FULL STACK REQUIREMENTS SUMMARY
# ═══════════════════════════════════════════════════════════════
# This repository contains a Full-Stack application.
#
# 1. FRONTEND: TypeScript, React, and CSS (Vite & Tailwind)
#    -> All frontend dependencies are managed via npm.
#    -> Please refer to `frontend/package.json` for TypeScript, UI, and CSS packages.
#    -> Install via: `cd frontend && npm install`
#
# 2. BACKEND: Python (FastAPI, YOLOv8 ML, Physics Engine)
#    -> All Python dependencies are managed via pip.
#    -> Listed below. Install via: `pip install -r requirements.txt`
# ═══════════════════════════════════════════════════════════════

# ── Backend (FastAPI + WebSocket Server) ──
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=14.0
python-multipart>=0.0.18

# ── ML / Computer Vision ──
ultralytics>=8.3.0          # YOLOv8 training + export
onnxruntime-directml>=1.20  # AMD Ryzen AI / DirectML inference
opencv-python-headless>=4.11.0
torch>=2.5.0
torchvision>=0.20.0

# ── Scientific Computing ──
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.14.0
scikit-learn>=1.6.0

# ── Visualization & Dashboard ──
streamlit>=1.41.0
pydeck>=0.9.0
matplotlib>=3.9.0
seaborn>=0.13.0
pillow>=11.0.0

# ── Data Pipeline ──
requests>=2.32.0

# ── LSTM Model Export ──
onnx>=1.15.0

# ── Supabase (used for data sync) ──
supabase>=2.11.0
